Transliterate this Arabic sentence into Latin script.

Respond only with JSON following this exact structure, no markdown formatting, no
triple ticks, nothing else than the JSON structure.

```json
{
  "transliteration": "ana naqra fi ktab"
}
```

Sentence: "<arabicText>"
