# Secrets / local config overrides — use Doppler, not .env
.env
*.local.yaml

# Editor
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# direnv
.direnv/

.scratch

# pg_dump backups (mounted into db container)
backups/*.dump
backups/*.sql
backups/*.gz
.superpowers/
.worktrees/
