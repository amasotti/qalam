type: object
required:
  - id
  - mode
  - status
  - items
  - createdAt
properties:
  id:
    type: string
    format: uuid
  mode:
    type: string
    enum:
      - NEW
      - LEARNING
      - KNOWN
      - MIXED
  status:
    type: string
    enum:
      - ACTIVE
      - COMPLETED
  items:
    type: array
    items:
      $ref: ./ExerciseSessionItemResponse.yaml
  createdAt:
    type: string
    format: date-time
  completedAt:
    type: string
    format: date-time
    nullable: true
